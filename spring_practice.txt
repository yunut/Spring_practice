spring은 xml의 지시문을 통해 객체를 생성하고, 모듈화된 부품을 적용시킬수 있다.

spring 구문 규칙에 property name는 setter(ex: setExam)을 set을 빼고 앞을 소문자로 적는게 규칙, exam

xml파일을 참조해 IoC컨테이너를 만들고 이 컨테이너에서 Bean을 가져와 객체로 만들수 있음

값형식을 지정할때 Index 또는 name으로 지정된 속성에 값을 넣을수 있음.

p: 태그 생성자를 활용해 각속성에 값을 지정할수 있음.